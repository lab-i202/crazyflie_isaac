# Optional local Crazyflie USD

Isaac Sim normally provides `cf2x.usd` in its asset library. If the built-in asset
cannot be found, place a compatible file here as:

`assets/robots/crazyflie/cf2x.usd`

Alternatively set `environment.crazyflie.usd_path` in the JSON configuration.
