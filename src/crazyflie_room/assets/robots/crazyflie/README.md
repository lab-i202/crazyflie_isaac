# Crazyflie local USD folder

Put `cf2x.usd` here only if Isaac Sim cannot find the built-in Crazyflie asset.

Default expected local path from the profile:

```text
assets/robots/crazyflie/cf2x.usd
```

The default profile first checks Isaac Sim built-in asset locations. This folder is the local fallback path.
